# 更新日志

## v1.0.0 (2026-06-01)

### 功能
- **已报销发票库**：导入已报销发票 PDF，自动提取发票号并持久化存储
- **待检查发票导入**：选择待检查的新发票 PDF，自动提取发票号
- **重复比对**：一键比对新发票与已报销库，左右两侧重复发票号同时红色高亮
- **数据持久化**：已报销库自动保存到 `~/.invoice_duplicate_db.json`，下次启动自动加载
- **删除记录**：支持删除选中发票号，支持一键删除全部记录（含确认弹窗）
- **作者签名**：窗口标题标注 `by Carmen`

### 模块
- `pdf_reader.py` — PDF 文本提取（pdfplumber）
- `invoice_extractor.py` — 正则提取发票号码
- `db.py` — 已报销发票数据库（JSON 持久化）
- `gui.py` — tkinter 双表格布局界面
- `app.py` — 应用入口
